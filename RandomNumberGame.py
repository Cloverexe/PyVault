# From what I understand right now this just imports the random function so a random whatever can be used.
# In this case, random.randint
import random
# This functions makes the computer select and print a random number between 1 and 100 and prints the 
# value selected.
def random_number():
    rand = random.randint(1, 100)
    print(rand)
    
num = 3    
random_number
